<ul class="picture">
  {{#each fighters}}
  <li class="character-{{attributes.id}}">
    <img src="{{attributes.image}}" />
  </li>
  {{/each}}
</ul>

<table>
  <thead>
    <tr>
      <td>Name</td>
      <td>HP</td> 
      <td>Actions</td>
      <td>PA</td>
    </tr>
  </thead>
  <tbody>
    {{#each fighters}}
    <tr id="character-{{attributes.id}}" class="character">
      <td class="name">{{attributes.name}}</td>
      <td class="life"></td> 
      <td class="actionButton"></td>
      <td class="actionPoint">{{attributes.energy.current}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
